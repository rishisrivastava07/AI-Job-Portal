server:
  port: ${SERVER_PORT:5001}

spring:
  application:
    name: job-portal-user-service

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:job_portal_user}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:rishi@123}
    driver-class-name: org.postgresql.Driver

  jwt:
    secret: ${JWT_SECRET:dsjkhdsfhdsfhdguw3b373q82h**277@HjGY@gy2*erwerefef}
    expiration: ${JWT_EXPIRATION:86400000}
